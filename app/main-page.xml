<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo">
  <StackLayout>
    <Label text="Tap the register" class="title"/>
    <Button text="Register" tap="{{ onTap }}" />
  </StackLayout>
</Page>
